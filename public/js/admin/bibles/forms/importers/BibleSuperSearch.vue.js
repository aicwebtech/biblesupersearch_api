import Base from './Base.vue.js';

var tpl = ``;

export default {
    template: tpl,
    extends: Base,

    data() {
        return {
            name: 'BibleSuperSearch',
            hasForm: false,
            hasContent: false
        }
    },
};