import Base from './Base.vue.js';

var tpl = ``;

export default {
    template: tpl,
    extends: Base,

    data() {
        return {
            name: 'MySword',
            hasForm: false,
            hasContent: false
        }
    },
};