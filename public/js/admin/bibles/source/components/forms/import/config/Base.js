enyo.kind({
    name: 'BibleManager.Components.Forms.Import.Config.Base',
    classes: 'input_config',
    configProps: {},

    components: [
        {tag: 'hr'}
    ],

    validate: function() {
        return true;
    }
});
